#include <stdio.h>
#include <stdlib.h>

int main()
{ int n;
float s,i;
   printf("enter n: number");
   scanf("%d",&n);
   s=0;
   for(i=1;i<=n;i++);
   s=s+pow(10,i);
   printf("%.2f",s);
    return 0;
}
